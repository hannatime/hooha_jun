<div class="row">
  <div class="col-sm-6 hidden-xs">
    <h1>Join Hooha Now</h1>
    <h2>Have Hooha CRM on us.</h2>
    <p>Yeah, Hooha's pretty new. We're really excited about helping small business sales people do better business. So for a limited time Hooha is free. If you sign up now, we'll make it free for the next 12 months.
    </p>
  </div>

  <div class="col-sm-6">
    <%= devise_error_messages! %>
    <%= hidden_field_tag 'plan', params[:plan] %>
    <%= display_base_errors resource %>
    
    <%if params[:plan] == 'free' %>
      <%= form_for(resource, :as => resource_name, 
      :url => registration_path(resource_name), 
      :html => { :class => "card_form-vertical" }) do |f| %>

    <h1>Free for 12 Months</h1>

      <div class="form-group">
        <div class="row">
              <%= f.label :username %>
              <%= f.text_field :username, :autofocus => true, class: "form-control" %>  
        </div>
      

        <div class="row">
              <%= f.label :email %>
              <%= f.email_field :email, :required => true, class: "form-control" %>  
        </div>


        <div class="row">       
              <%= f.label :password %>
              <%= f.password_field :password, :required => true, class: "form-control" %>
        </div>

        <div class="row">
              <%= f.label :password_confirmation %>
              <%= f.password_field :password_confirmation, :required => true, class: "form-control" %>
        </div>

          <div class="row submit-button">
            <%= f.submit "12 Months Free", class: "btn btn-block btn-lg btn-warning" %>
          </div> 
        <%= render "devise/shared/links" %>
        
        </div> 
        <% end %>
      <% end %>
       

    </div>
  </div>
</div>